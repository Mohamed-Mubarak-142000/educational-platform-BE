// Legacy payment routes removed — all payment functionality now handled by paymobRoutes.ts
// This file is kept as a placeholder for any legacy compatibility needs.
import { Router } from "express";
const router = Router();
export default router;
